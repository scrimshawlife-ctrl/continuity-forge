# Continuity Forge 1.5.1

**Handoff polish** — file import, MCP coverage, harness in merge gate

| Field | Value |
|-------|--------|
| Version | `1.5.1` |
| Tag | `v1.5.1` |
| Base | **v1.5.0** handoff release |
| Package / API | `1.5.1` |
| Breakdown schema | `cf.breakdown.v1` (unchanged) |

Patch release on the handoff stream. No breaking changes to the connector package.

---

## Changes

| Item | Detail |
|------|--------|
| **UI file import** | **Import file** button + drag-and-drop onto script field (`.fountain` / `.fdx` / `.txt`) |
| **MCP tests** | Contract coverage for `build_breakdown` / `build_breakdown_markdown` |
| **Merge gate** | `make validate` runs `scripts/handoff_harness.py` after coverage floors |
| **Docs** | Handoff guide notes import + gate |

---

## Pin

```bash
git checkout v1.5.1
pip install -e '.[dev]'
make validate   # includes handoff harness
make ui         # Import file → Build breakdown → download JSON
```

---

## Support policy

| Stream | Policy |
|--------|--------|
| `v1.4.0` | Baseline freeze |
| `v1.5.0` | First handoff release |
| **`v1.5.1`** | This polish (recommended handoff pin) |
| `1.5.x` | Further handoff bugfixes |
| `1.6+` | Production stack / live providers |

---

## Non-claims

Unchanged from 1.5.0: not production film; structure + continuity only.
